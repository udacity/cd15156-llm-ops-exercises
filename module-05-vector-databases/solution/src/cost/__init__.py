"""Cost monitoring package — JSONL log + summarization + HTML dashboard.

The package splits into ``tracker`` (pure functions over the JSONL
log) and ``dashboard`` (FastAPI router rendering an HTML report).
"""
